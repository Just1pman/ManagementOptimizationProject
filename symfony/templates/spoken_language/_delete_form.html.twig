<form method="post" action="{{ path('spoken_language_delete', {'id': spoken_language.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ spoken_language.id) }}">
    <button class="btn">Delete</button>
</form>
