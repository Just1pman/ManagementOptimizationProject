{% extends 'base.html.twig' %}

{% block title %}ManagerData index{% endblock %}

{% block body %}
    <h1>ManagerData index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>UserRole</th>
                <th>UserSalary</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for manager_datum in manager_datas %}
            <tr>
                <td>{{ manager_datum.id }}</td>
                <td>{{ manager_datum.userRole }}</td>
                <td>{{ manager_datum.userSalary }}</td>
                <td>
                    <a href="{{ path('manager_data_show', {'id': manager_datum.id}) }}">show</a>
                    <a href="{{ path('manager_data_edit', {'id': manager_datum.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('manager_data_new') }}">Create new</a>
{% endblock %}
