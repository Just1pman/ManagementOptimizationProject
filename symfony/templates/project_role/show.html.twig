{% extends 'base.html.twig' %}

{% block title %}ProjectRole{% endblock %}

{% block body %}
    <h1>ProjectRole</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ project_role.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ project_role.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('project_role_index') }}">back to list</a>

    <a href="{{ path('project_role_edit', {'id': project_role.id}) }}">edit</a>

    {{ include('project_role/_delete_form.html.twig') }}
{% endblock %}
