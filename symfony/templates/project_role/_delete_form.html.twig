<form method="post" action="{{ path('project_role_delete', {'id': project_role.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ project_role.id) }}">
    <button class="btn">Delete</button>
</form>
