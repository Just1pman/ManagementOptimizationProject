{{ form_start(form) }}
{#    {{ form_widget(form) }}#}
    {{ form_row(form.email) }}
{#    {{ form_widget(form) }}#}
    {{ form_row(form.password) }}


    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
